# 盛安密封官网 · 交付与部署文档

> 交付版本：2026-07-29（对应 Git 提交 `77c3e30`，含客户真实数据 + 后台二维码管理功能）
> 技术栈：React 18 + Vite（前端） / Flask + Gunicorn（后端） / SQLite（数据库，零配置） / Nginx（网关）
> 适用对象：**人类运维** + **AI 部署智能体** 均可照此文档直接完成部署与验收

---

## 0. 一句话说明（先读这段）

这是一个**企业产品官网 + 内容管理后台**的完整交付物：

- **前端**：React 单页应用（首页 / 产品中心 / 产品详情 / 后台登录）
- **后端**：Flask 提供 REST API + 后台管理 + SEO（sitemap/robots）
- **数据库**：SQLite 单文件（`shengan.db`），**无需安装 MySQL、无需建库建表**
- **本交付包已包含客户的真实业务数据**（`shengan.db`）和全部图片，**部署即同线上内容**，不是空站

| 角色 | 该看哪里 |
|------|----------|
| 人类运维，想了解项目 | 第 1、2 节 |
| 任何人（含 AI）要快速部署 | 第 3 节 |
| 验证「功能是否全部跑通」 | 第 4 节 |
| 上线后日常运维 / 排错 | 第 5、6 节 |

---

## 1. 项目功能清单（即「功能全部实现」的含义）

部署完成后，以下能力均应可用。第 4 节给出逐项验证命令。

### 1.1 前台（用户可见）
- **首页**：企业介绍、产品展示、新闻动态、联系方式等模块（长页）
- **产品中心** `/products`：产品列表、分类筛选
- **产品详情** `/products/:id`：产品图册、参数说明、**真实联系二维码展示**（最新功能）
- **留言提交**：访客留言（写入 `messages` 表）
- **样本申请**：访客提交样品申请（写入 `samples` 表）
- **SEO**：`/sitemap.xml`、`/robots.txt` 由后端动态生成

### 1.2 后台管理 `/admin`
- **账号管理**：管理员增删改、改密码、启停状态、登录日志
- **产品管理**：产品 CRUD、批量导入/导出（Excel/CSV）
- **子产品管理**：产品子系列 CRUD
- **新闻管理**：资讯 CRUD、批量导入/导出
- **留言管理**：查看、处理、删除访客留言
- **样本申请管理**：查看、处理、删除样品申请
- **二维码管理**：后台上传/维护联系二维码，前台详情页展示
- **系统设置**：站点 URL、SEO 信息等
- **数据看板**：后台统计接口

### 1.3 系统能力
- SQLite 持久化（零运维数据库）
- 图片静态服务（`/images/`，30 天缓存）
- CORS 可配置（`CORS_ORIGINS` 环境变量）
- systemd 守护（崩溃自动重启、开机自启）

---

## 2. 交付物清单

解开 `shengan-seal-源码交付-20260729.zip` 后得到 `shengan-seal/` 目录：

| 内容 | 说明 |
|------|------|
| `backend/` | Flask 后端（`app.py` / `init_db.py` / `requirements.txt` / `shengan.db` 真实数据） |
| `src/` | React 前端源码 |
| `images/` | 客户全部图片（约 15MB，与数据库引用一一对应） |
| `dist/` | ⚠️ 解压后**可能不存在**（部署时 `npm run build` 生成） |
| `deploy.sh` | 一键部署脚本（可重复执行，保留数据库） |
| `DEPLOY.md` | 精简技术部署手册 |
| `backend/shengan.db` | **客户真实数据**：14 产品 / 4 新闻 / 2 留言 / 2 样本 / 2 管理员 |

**敏感/凭据说明**：
- `backend/.secret_key`、`backend/.admin_pass` **不在交付包内**（部署首次启动自动生成）。
- `shengan.db` 含管理员密码的哈希，部署后**请务必改一次后台密码**（见第 6 节）。

---

## 3. 快速部署（AI 可一键执行 / 人类分步）

> 部署目录固定为 `/var/www/shengan-seal`，`deploy.sh` 写死了该路径。
> 需要 **root** 权限。目标系统：Ubuntu 20.04+ / Debian 11+（CentOS 7+ 亦可）。

### 方式 A：deploy.sh 一键部署（推荐，最简单）

```bash
# 1) 把 shengan-seal 目录上传到服务器 /var/www/
scp -r shengan-seal root@服务器IP:/var/www/
#    或先传 zip 再解压：unzip shengan-seal.zip -d /var/www/

# 2) 进入目录，一键部署
cd /var/www/shengan-seal
sudo bash deploy.sh

# 3) 部署完成后访问（默认端口 8082）
#    前台： http://服务器IP:8082/
#    后台： http://服务器IP:8082/#/admin
```

脚本自动完成：装系统依赖 → 建 Python venv 并装依赖 → **检测已有 shengan.db 则复用真实数据（不重置）** → 前端构建 → 配置 systemd 后端 → 配置 nginx（监听 8082）→ 开防火墙。

### 方式 B：手动分步（便于 AI 排错 / 理解每一步）

```bash
# ① 系统依赖
apt-get update -qq
apt-get install -y -qq nginx python3 python3-pip python3-venv nodejs npm git curl
#   （CentOS 用 yum install -y nginx python3 python3-pip nodejs npm git curl）

APP_DIR=/var/www/shengan-seal
cd "$APP_DIR"

# ② Python 后端环境
python3 -m venv backend/venv
source backend/venv/bin/activate
pip install -q -r backend/requirements.txt   # 仅 flask / flask-cors / gunicorn

# ③ 数据库：交付包已含 shengan.db（真实数据），存在即跳过，不要删！
#    若想全新空站再灌种子，才执行：rm -f backend/shengan.db && (cd backend && python init_db.py)
ls -l backend/shengan.db   # 确认真实数据在位

# ④ 前端构建（默认根部署 VITE_BASE_PATH=/）
export VITE_BASE_PATH=/
npm install --silent
npm run build               # 产物输出到 dist/

# ⑤ 配置 systemd 后端服务（完整文件，可直接写入）
cat > /etc/systemd/system/shengan-seal.service << 'EOF'
[Unit]
Description=Shengan Seal Website Backend (Flask)
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/shengan-seal/backend
ExecStart=/var/www/shengan-seal/backend/venv/bin/gunicorn -w 4 -b 127.0.0.1:5000 app:app
Restart=always
RestartSec=5
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
EOF
systemctl daemon-reload
systemctl enable --now shengan-seal

# ⑥ 配置 nginx（根部署完整配置，可直接写入）
cat > /etc/nginx/sites-available/shengan-seal << 'EOF'
server {
    listen 8082;
    server_name _;

    root /var/www/shengan-seal/dist;
    index index.html;

    # SEO 专用：sitemap/robots 走后端
    location = /sitemap.xml { proxy_pass http://127.0.0.1:5000/api/sitemap.xml; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; add_header Content-Type "application/xml; charset=utf-8"; expires 1h; }
    location = /robots.txt { proxy_pass http://127.0.0.1:5000/api/robots.txt; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; }

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 25M;
        proxy_read_timeout 300s;
    }

    # 图片资源（30 天缓存）
    location ^~ /images/ {
        root /var/www/shengan-seal;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 前端 SPA
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
EOF
ln -sf /etc/nginx/sites-available/shengan-seal /etc/nginx/sites-enabled/shengan-seal
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl reload nginx

# ⑦ 防火墙
ufw allow 8082/tcp 2>/dev/null || iptables -I INPUT -p tcp --dport 8082 -j ACCEPT 2>/dev/null || true
```

### 绑定域名 + HTTPS（生产必做）

```bash
# 1) 域名 A 记录指向服务器 IP
# 2) 改 nginx 监听 80 并绑定域名
sed -i 's/listen 8082;/listen 80;/; s/server_name _;/server_name 你的域名 www.你的域名;/' \
    /etc/nginx/sites-available/shengan-seal
nginx -t && systemctl reload nginx
# 3) 申请免费 SSL（certbot 会自动把配置改写为 443）
apt-get install -y certbot python3-certbot-nginx
certbot --nginx -d 你的域名 -d www.你的域名
```

> 端口说明：默认 `8082` 是为避开目标机上可能已占用 80/443 的面板（如 aaPanel）。若服务器是干净系统，直接把 nginx 改成 `listen 80` 即可，无需改后端。

---

## 4. 部署后功能验收（确认「功能全部实现」）

**4.1 服务与端口**

```bash
systemctl is-active shengan-seal     # 应为 active
ss -ltnp | grep -E '5000|8082'        # 后端 5000 + nginx 8082 在监听
```

**4.2 后端 API 返回真实数据（核心验证）**

```bash
# 产品（应为 14 条 JSON 数组）
curl -s http://127.0.0.1:5000/api/products | head -c 200; echo
# 新闻（应为 4 条）
curl -s http://127.0.0.1:5000/api/news | head -c 200; echo
# 系统设置
curl -s http://127.0.0.1:5000/api/settings | head -c 200; echo
# SEO
curl -s http://127.0.0.1:5000/api/sitemap.xml | head -c 120; echo
```

**4.3 前端页面 + 图片**

```bash
# 首页应返回最新构建的 index.html
curl -s http://127.0.0.1:8082/ | grep -o 'assets/.*\.js' | head -1
# 图片不应 404（取一条产品引用的图片验证，下面用 store_logo 举例）
curl -I http://127.0.0.1:8082/images/store_logo.webp | head -1
```

**4.4 后台登录**

- 浏览器打开 `http://服务器IP:8082/#/admin`
- 管理员账号 `admin`，**密码在首次启动时随机生成并写入 `backend/.admin_pass`**：
  ```bash
  cat /var/www/shengan-seal/backend/.admin_pass
  ```
- 登录后应能看到：产品管理、新闻管理、留言/样本管理、二维码管理、系统设置等菜单。

**4.5 功能验收清单（逐项打勾）**

- [ ] 首页正常、图片不裂
- [ ] 产品中心能看到 14 款产品，详情页能看二维码
- [ ] 新闻动态有内容
- [ ] `/api/messages` 提交留言成功（后台可见）
- [ ] `/api/samples` 提交样本申请成功（后台可见）
- [ ] `/admin` 能登录，后台各管理模块可操作
- [ ] `/sitemap.xml`、`/robots.txt` 可访问
- [ ] 重启服务器后网站自动恢复（systemd 已 enable）

---

## 5. 运维命令速查

```bash
systemctl status  shengan-seal     # 后端状态
systemctl restart shengan-seal     # 重启后端
journalctl -u shengan-seal -f      # 后端日志
nginx -t && systemctl reload nginx # 重载 nginx

# 升级代码（保留数据库和密钥）：上传新代码后重跑
cd /var/www/shengan-seal && sudo bash deploy.sh

# 数据备份（只需备这两样 + 图片）
cp backend/shengan.db /root/shengan-$(date +%Y%m%d).db
tar czf /root/images-$(date +%Y%m%d).tgz images/
```

---

## 6. 安全须知（部署后必做）

1. **改后台密码**：用 `.admin_pass` 里的随机密码登录后，在「账号管理」改成强密码。
2. **保留 `.secret_key`**：这是 Flask 会话密钥，首次启动自动生成，升级代码时切勿删除（删除会重置所有会话）。
3. **禁止 IP 直访（建议）**：在 nginx `server` 块加
   ```nginx
   if ($host != $server_name) { return 301 https://$server_name$request_uri; }
   ```
4. **数据库文件权限**：`shengan.db` 含业务数据，建议 `chmod 600 backend/shengan.db`。

---

## 7. 常见问题

**Q1：首页 502**
`systemctl status shengan-seal` 看后端是否活着；`journalctl -u shengan-seal -n 50` 看报错。常见原因：5000 端口被占 → 改 `deploy.sh` 顶部 `BACKEND_PORT` 后重跑脚本。

**Q2：图片 404**
确认 `/var/www/shengan-seal/images/` 存在且权限 755；`curl -I http://127.0.0.1:8082/images/store_logo.webp` 应为 200。

**Q3：产品中心 0 款产品**
`curl http://127.0.0.1:5000/api/products` 应为 JSON 数组。若为空且确认 `shengan.db` 被误删，可 `cd backend && venv/bin/python init_db.py` 灌种子（**会生成空站 14 款种子，覆盖不了客户真实数据，慎用**）。

**Q4：前端资源 404（路径带 /shengan/）**
说明 `.env.production` 里 `VITE_BASE_PATH` 不是 `/`。改回 `VITE_BASE_PATH=/`，重跑 `npm run build` 或 `bash deploy.sh`。

**Q5：sitemap.xml 里 URL 不对**
后台「系统设置」填站点 URL；或在 `/etc/systemd/system/shengan-seal.service` 加 `Environment="SHENGAN_SITE_URL=https://你的域名"`，然后 `systemctl daemon-reload && systemctl restart shengan-seal`。

**Q6：忘记后台密码**
`.admin_pass` 仅在 `admin` 账号**首次创建**时生效；已有账号需重置：
```bash
cd /var/www/shengan-seal/backend
echo '新密码' > .admin_pass && chmod 600 .admin_pass
venv/bin/python -c "import sqlite3; db=sqlite3.connect('shengan.db'); db.execute('DELETE FROM admin_users'); db.commit()"
systemctl restart shengan-seal   # 重启后按 .admin_pass 重建 admin 账号（仅清空账号表，业务数据不受影响）
```
