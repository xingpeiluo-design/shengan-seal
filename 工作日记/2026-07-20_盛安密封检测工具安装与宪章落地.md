# 盛安密封·检测工具安装与宪章落地

关联工作日记：2026-07-20_盛安密封后台入口隐藏与账号加密.md

## 一、目标
用户（小白）要求：安装能检测「前端硬编码 / 前后端错位 / 启动建 SQL」的工具与插件，用它们扫描并修复盛安密封，睡觉前全权交付。

## 二、工具清单真伪甄别（重要）
用户贴的清单里**混有 AI 编造条目**，已甄别：
- ✅ 真实可用：SonarLint（VSCode 插件，本环境装不了，需用户在 VSCode 一键装）、ESLint、Semgrep（开源静态扫描）、Spectral（OpenAPI 校验，盛安无 OpenAPI 故不适用）、Playwright（动态业务测试）、Postman、Testcontainers。
- ❌ 编造/不实：`github.com/yourduskquibbles/ai-code-audit`（假仓库链接）、`fullstack-linter`（非真实项目，泛称）；Spec-Kit **并无**"Frontend Hardcode Check / API Consistency / DB Migration Safety"三个内置 Skill（它只有 /constitution /specify /plan /tasks /validate，且 /validate 校验 spec 符合度，非那三个检测器）；"Cursor/Claude 自定义提示词 Skill"只是段 prompt 模板。
- 已安装的 specify-cli 0.13.1.dev0 真实可用。

## 三、实际安装并验证的检测工具
1. **Semgrep 1.170.0**：`uv tool install semgrep`（真实，可执行 `semgrep`/`pysemgrep`）。
2. **ai-site-audit 扫描器**（已有）增强：新增 `semgrep-rules.yaml` 三条 CI 规则（dangerous-db-reinit / hardcoded-secret-or-phone / plaintext-password-hash），并修正扫描器「兜底值 * 标注」逻辑（之前 ContactFooter:134 漏标已修复，确认工具准确）。
3. **Spec-Kit 宪章**：`specify init . --ignore-agent-tools --force` 搭好 `.specify/` 脚手架，并写入项目宪法 `.specify/constitution.md`，把三条红线 + 账号加密要求 + 提交前跑双扫描作为质量门控写进宪法。

## 四、盛安密封最终扫描结果（复测）
- **ai-site-audit**：
  - 【1】前端硬编码 24 处，全部为 `settings.x || '兜底值'` 形式（标 `*`），生产代码无真硬编码；余 index.html 的 SEO 电话（见下修复）。
  - 【2】危险数据库初始化 **0 处**（init_db.py 已无 DELETE FROM，建表全用 IF NOT EXISTS×8，种子 COUNT 守卫）。
  - 【3】前后端字段错位疑似 3 个（data_key/for_each/is_route，框架内部属性误报，可忽略）。
- **Semgrep（本地规则）**：dangerous-db-reinit **0**、plaintext-password-hash **0**（已升级 PBKDF2+盐）；5 个命中全是 fallback 电话/种子/测试。

## 五、本轮修复
- `src/App.tsx`：新增 `useSettings()` + effect，运行时用后台 `settings.hotline` 覆盖 `index.html` 的 meta description / og:description / JSON-LD telephone，解决 SEO 电话写死（之前唯一真残留）。`npm run build` 通过（62 模块）。
- 之前轮次已修复：前端硬编码读 settings、双种子统一+幂等、启动不建 SQL、后台入口隐藏、账号 PBKDF2 加密、`.gitignore` 忽略密钥。

## 六、验证
| 项 | 结果 |
|---|---|
| 前端 npm run build | ✅ 62 模块 |
| ai-site-audit 【2】危险SQL | ✅ 0 |
| Semgrep dangerous-db-reinit | ✅ 0 |
| Semgrep plaintext-password-hash | ✅ 0 |
| ai-site-audit 【1】生产代码真硬编码 | ✅ 0（仅 fallback/index.html SEO 已同步） |

## 七、未做（风险可控，待用户确认）
- **未 git commit / 未部署**：改动均在本地文件，dist 已 rebuild 但未上线到小红 VPS。用户醒来后建议：git commit → 部署 dist → 人工业务测试（后台改电话看前台/SEO 是否同步、加 2 条数据停启后端是否保留）。
- 线上 admin 仍用密码 `shengan2026`，部署后首次登录会自动升级 PBKDF2 哈希，但密码本身建议改为强密码（后台「账号管理」或 ssh xiaohong 用 app 内置 _hash_password 设置）。
- SonarLint 需用户在 VSCode 自行安装（本 CLI 环境无法装 GUI 插件）。
