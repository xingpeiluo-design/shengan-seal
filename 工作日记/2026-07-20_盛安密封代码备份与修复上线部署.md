# 盛安密封代码备份与修复上线部署 2026-07-20

关联工作日记：2026-07-20_盛安密封三大问题修复与Spec-Kit安装.md

## 目标
将已修复的盛安密封代码（三大问题 + 后台安全加固）备份到本地 git 与 GitHub，并部署上线到小红 VPS（xiaohong / 139.224.186.15），让修复在生产环境生效。

## 假设
- 之前所有代码修复已在本地完成、未提交未部署。
- 线上数据库 `shengan.db` 为真实业务数据，必须保留；`.secret_key` 必须保留。
- 线上 `admin_users` 表已存在且非空，`admin` 密码为 `shengan2026`（旧 sha256）。

## 操作记录
1. **本地 + GitHub 备份**：`git add` 源码/配置/.specify/.github/工作日记；commit `dc195b9`「fix: 修复盛安密封三大问题并加固后台安全」；`git push origin main`（55 文件，+4993/-160）。
2. **前端构建**：`npm run build`，vite `base=/shengan/`（与 `.env.production` 一致，匹配线上子路径部署），dist 生成 62 模块。
3. **VPS 回滚快照**：`/root/shengan_backups/shengan-seal.20260720_231000.tgz`（16M，排除 venv）。
4. **rsync 同步**（排除 `.git` / `node_modules` / `backups` / `工作日记` / `images` / 数据库 / `.secret_key` / `.admin_pass` / `venv` 等）：本地 → `xiaohong:/var/www/shengan-seal/`。
5. **重启后端**：`rm -rf backend/__pycache__`；`systemctl restart shengan-seal`（active）。
6. **验证**：
   - `/api/settings` 返回数据库内容；`/api/products` 返回 14 条。
   - 公开站点 `https://maichewei.com/shengan/` → 200；`/shengan/assets/index--LknYP5E.js` → 200（新构建哈希，证明服务的是新前端）。
   - 登录 `admin/shengan2026` → 返回 token（未被锁死）；登录后 `admin` 密码哈希由旧 sha256 **静默升级为 `pbkdf2_sha256$200000$…`**（PBKDF2 + 随机盐 + SECRET_KEY pepper）。

## 修改范围
- `backend/app.py`：账号体系 PBKDF2 加密 + 兼容旧 sha256 静默升级；`init_db` 幂等（COUNT 守卫，启动不再清空重建 SQL）。
- `backend/init_db.py`：种子数据 COUNT 守卫幂等。
- `src/*`：组件改读 settings（消除硬编码）；`Header` 移除可见 admin 入口；`App.tsx` SEO 元信息随 settings 同步。
- `.specify/constitution.md`：三条红线（前端不硬编码 / 字段命名统一 / 启动不重建 SQL）+ 后台加密 + 提交前扫描门禁。
- `.github/`：Spec-Kit 工作流与 agents。

## 验证结果
全部通过：三大问题修复生效、后台加密生效、admin 入口隐藏、数据库零丢失、站点可访问。

## 未验证项
- 未做浏览器端人工业务走查（留言 / 寄样 / 产品编辑等后台操作），建议罗东登录 `https://maichewei.com/shengan/admin` 实际操作一遍。
- 未单独配置 SSL：当前 `/shengan/` 走 maichewei.com 主站 443 证书。

## 注意事项
- **强烈建议**登录后台将默认密码 `shengan2026` 改为强密码（后台 → 账号管理 / 修改密码）。
- 本次用 rsync 部署而非 `deploy.sh`：本机 VPS 的 nginx 由 aaPanel 管理（`/www/server/nginx/conf/nginx.conf` + `/www/server/panel/vhost/nginx/*.conf`），`systemctl nginx` 是无效 sysv 桩，`deploy.sh` 里的 `systemctl reload nginx` 在此环境会失败。本次**未改动 nginx 配置**（公开 `/shengan/` 子路径已由 maichewei.com.conf 正确服务）。
- 回滚方法：`tar xzf /root/shengan_backups/shengan-seal.20260720_231000.tgz -C /var/www`（覆盖当前代码与 dist，保留 venv），再 `systemctl restart shengan-seal`。
