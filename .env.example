# 盛安密封官网前端环境变量模板
# 复制本文件为 .env.development（本地开发）和 .env.production（生产部署）
# 然后根据实际部署场景修改
#
# Vite 配置读取规则：
#   - VITE_* 前缀的变量会被注入到前端代码中（import.meta.env.VITE_*）
#   - build 时会读取对应 mode 的 .env.{mode} 文件

# ============ 部署路径（前端静态资源前缀） ============
# HTML 中 <base href> 和所有静态资源（图片/CSS/JS）的相对路径前缀
#
# 场景 1: 客户独立域名根部署（推荐·用于客户 VPS）
#   VITE_BASE_PATH=/
#   访问地址: https://shengan.example.com/
#   → 图片请求: https://shengan.example.com/images/...
#
# 场景 2: 在主站子路径下部署（当前生产环境）
#   VITE_BASE_PATH=/shengan/
#   访问地址: https://www.maichewei.com/shengan/
#   → 图片请求: https://www.maichewei.com/shengan/images/...
#
# 场景 3: 其他子路径
#   VITE_BASE_PATH=/your-subpath/
#   访问地址: https://example.com/your-subpath/
VITE_BASE_PATH=/shengan/

# ============ 站点绝对 URL（SEO 相关）============
# 用于：
#   - <link rel="canonical">  规范化 URL
#   - <meta property="og:url">  社交分享 URL
#   - JSON-LD 结构化数据中的 url / logo
#   - 后端 sitemap.xml / robots.txt（通过环境变量 SHENGAN_SITE_URL）
#
# ⚠️ 这个值必须与 VITE_BASE_PATH 对应的实际访问地址一致
#    （即用户在浏览器地址栏看到的 URL）
#
# 场景 1: 客户独立域名根部署
#   VITE_SITE_URL=https://shengan.example.com/
#
# 场景 2: 当前主站子路径
#   VITE_SITE_URL=https://www.maichewei.com/shengan/
VITE_SITE_URL=https://www.maichewei.com/shengan/

# ============ 后端环境变量（部署时同步设置） ============
# 这些不是 Vite 读取的，是在后端服务运行时设置的
# 例如在 systemd unit / docker-compose / 启动脚本里：
#
#   export SHENGAN_SITE_URL=https://shengan.example.com/  # 与 VITE_SITE_URL 一致
#   export SHENGAN_SECRET_KEY=...                          # 强随机密钥（首次启动后会自动持久化）